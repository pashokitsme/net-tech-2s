$TTL 86400
@       IN      SOA     ns.internal. admin.internal. (
                        2023040101      ; Serial
                        3600            ; Refresh
                        1800            ; Retry
                        604800          ; Expire
                        86400 )         ; Minimum TTL

; Name servers
@       IN      NS      ns.internal.

; PTR records
2.0     IN      PTR     ns.internal. 
